<!DOCTYPE html>
<html>
  <head>
    <title>Add Doctor</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-slate-900 text-white flex justify-center items-center min-h-screen"
  >
    <form
      method="POST"
      class="bg-slate-800 p-6 rounded w-full max-w-md space-y-4"
    >
      <h2 class="text-xl font-bold">Tambah Dokter</h2>

      <input
        name="doctorId"
        placeholder="Doctor ID"
        class="w-full p-2 bg-slate-700 rounded"
        required
      />
      <input
        name="name"
        placeholder="Nama"
        class="w-full p-2 bg-slate-700 rounded"
        required
      />
      <input
        name="specialization"
        placeholder="Spesialisasi"
        class="w-full p-2 bg-slate-700 rounded"
        required
      />

      <button class="w-full bg-blue-600 py-2 rounded">Simpan</button>
    </form>
  </body>
</html>
