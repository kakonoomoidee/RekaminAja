<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detail Rekam Medis – RekaminAja</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: ["Inter", "sans-serif"],
              mono: ["JetBrains Mono", "monospace"],
            },
          },
        },
      };
    </script>
  </head>
  <body class="bg-slate-950 text-slate-200 font-sans antialiased">
    <%- include("../partials/navbar") %>

    <main class="pl-64 min-h-screen">
      <div class="p-8 max-w-5xl mx-auto">
        <a
          href="/admin/medical?patientHash=<%= record.patientNikHash %>"
          class="text-sm text-slate-500 hover:text-blue-400 flex items-center gap-1 transition-colors mb-6"
        >
          <svg
            class="w-4 h-4"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M10 19l-7-7m0 0l7-7m-7 7h18"
            />
          </svg>
          Kembali ke Timeline
        </a>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
          <div class="lg:col-span-2 space-y-6">
            <div>
              <h1 class="text-2xl font-bold text-white">
                Laporan Medis (Decrypted)
              </h1>
              <p class="text-slate-400 text-sm mt-1">
                Berikut adalah data asli yang telah didekripsi dari blockchain.
              </p>
            </div>

            <div
              class="bg-slate-900 border border-slate-800 rounded-xl overflow-hidden shadow-2xl relative group min-h-[300px] flex flex-col"
            >
              <div
                class="h-1 bg-gradient-to-r from-blue-500 to-emerald-500 w-full relative z-10"
              ></div>

              <div class="p-6 relative flex-1 flex flex-col">
                <div
                  class="absolute inset-0 flex items-center justify-center opacity-20 pointer-events-none overflow-hidden"
                >
                  <span
                    class="text-6xl md:text-8xl font-black text-slate-700 uppercase tracking-tighter -rotate-12 select-none whitespace-nowrap"
                  >
                    CONFIDENTIAL
                  </span>
                </div>

                <div class="relative z-10 w-full flex-1">
                  <div
                    class="bg-slate-950/90 p-6 rounded-lg border border-slate-800/50 shadow-inner h-full backdrop-blur-sm"
                  >
                    <pre
                      id="jsonOutput"
                      class="font-mono text-sm leading-relaxed text-emerald-400 whitespace-pre overflow-x-auto"
                    ></pre>
                  </div>
                </div>
              </div>

              <div
                class="bg-slate-950 px-6 py-3 border-t border-slate-800 flex justify-between items-center text-xs text-slate-500 relative z-10"
              >
                <span>End of Document</span>
                <span class="flex items-center gap-1 text-emerald-500">
                  <svg
                    class="w-3 h-3"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                    />
                  </svg>
                  Verified Integrity
                </span>
              </div>
            </div>
          </div>

          <div class="space-y-6">
            <div
              class="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-lg"
            >
              <h3
                class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-3"
              >
                Timestamp
              </h3>
              <div class="flex items-center gap-3 text-white">
                <div class="p-2 bg-blue-500/10 rounded-lg text-blue-400">
                  <svg
                    class="w-5 h-5"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                    />
                  </svg>
                </div>
                <div>
                  <p class="font-medium">
                    <%= new Date(record.timestamp *
                    1000).toLocaleDateString('id-ID', { weekday: 'long', year:
                    'numeric', month: 'long', day: 'numeric' }) %>
                  </p>
                  <p class="text-sm text-slate-400">
                    <%= new Date(record.timestamp *
                    1000).toLocaleTimeString('id-ID') %> WIB
                  </p>
                </div>
              </div>
            </div>

            <div
              class="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-lg"
            >
              <h3
                class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-3"
              >
                Dokter Pemeriksa
              </h3>
              <div class="space-y-3">
                <div>
                  <p class="text-xs text-slate-500">Hash ID:</p>
                  <code
                    class="text-xs font-mono text-blue-300 break-all bg-blue-900/20 p-1 rounded block mt-1"
                  >
                    <%= record.doctorIdHash %>
                  </code>
                </div>
                <div class="pt-3 border-t border-slate-800/50">
                  <a
                    href="/admin/doctors/<%= record.doctorIdHash %>"
                    class="text-sm text-blue-400 hover:text-blue-300 flex items-center gap-1"
                  >
                    Lihat Profil Dokter &rarr;
                  </a>
                </div>
              </div>
            </div>

            <div
              class="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-lg"
            >
              <h3
                class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-3"
              >
                Security Metadata
              </h3>
              <ul class="space-y-2 text-sm text-slate-400">
                <li class="flex items-center gap-2">
                  <span class="w-1.5 h-1.5 rounded-full bg-green-500"></span>
                  Stored on Private Chain
                </li>
                <li class="flex items-center gap-2">
                  <span class="w-1.5 h-1.5 rounded-full bg-green-500"></span>
                  AES-256 Encrypted
                </li>
                <li class="flex items-center gap-2">
                  <span class="w-1.5 h-1.5 rounded-full bg-green-500"></span>
                  Immutable Ledger
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </main>

    <script>
      const rawData = `<%- record.decryptedData.replace(/\\/g, '\\\\').replace(/"/g, '\\"') %>`;

      const outputElement = document.getElementById("jsonOutput");

      try {
        const jsonObject = JSON.parse(rawData);
        const prettyJson = JSON.stringify(jsonObject, null, 2);

        outputElement.textContent = prettyJson;
      } catch (e) {
        console.error("Gagal parse JSON:", e);
        outputElement.textContent = rawData;
      }
    </script>
  </body>
</html>
