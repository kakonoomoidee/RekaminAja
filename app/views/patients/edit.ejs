<!DOCTYPE html>
<html>
  <head>
    <title>Edit Pasien</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-slate-900 text-white">
    <%- include("../partials/navbar") %>

    <div class="p-6 max-w-xl">
      <h1 class="text-xl font-bold mb-4">Edit Pasien</h1>

      <form method="POST" action="/admin/patients/<%= patient.nikHash %>/edit">
        <input
          name="name"
          value="<%= patient.name %>"
          class="w-full p-2 text-black"
        />
        <textarea name="address" class="w-full p-2 text-black"></textarea>

        <button class="bg-yellow-600 px-4 py-2 rounded">Update</button>
      </form>
    </div>
  </body>
</html>
